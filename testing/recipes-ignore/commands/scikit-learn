# This export is unnecessary, IMO
export OPENBLAS_NUM_THREADS=1
# Since `import sklearn` works, we can skip for now the full pytest test suite that fails
#pytest --verbose --pyargs sklearn
# Same test suite but for platforms [aarch64 or ppc64le]:
pytest --verbose --pyargs sklearn -k 'not test_estimate_bandwidth_1sample'